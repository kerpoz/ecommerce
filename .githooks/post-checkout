#!/bin/bash

echo "Running post-checkout hook"

# Run set-profile.sh to set the correct profile based on the branch
./set-profile.sh

# Log execution to a file for debugging
echo "Post-checkout hook executed" >> /tmp/post-checkout.log
date >> /tmp/post-checkout.log